import { ComingSoon } from '@sdarm/ui';

export const runtime = 'edge';

export default function EventsPage() {
  return <ComingSoon title="Events" subtitle="Veranstaltungen, Seminare und Gottesdienste — bald hier verfügbar." />;
}
