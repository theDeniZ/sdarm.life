node_modules
.wrangler
.env*
.dev.vars
.next
dist
**/tsconfig.tsbuildinfo
**/.turbo
.pnpm-store
.vscode/mcp.json
.mcp.json
.DS_Store
**/.obsidian
_tests
test-results/
playwright-report/
tests/screenshot/results/

# Local-only dev sketches, tuners, multi-device previews, design variants.
# Routes like /de/newsletter-variants, /de/planet-tuner, /de/footer-preview
# are work-in-progress and must never ship to production.
**/(dev)/

/.claude/worktrees
.claude-backup.tgz
tests/a11y/results/

# Local scratch that never belongs in the repo.
# sketches/ holds source photographs and standalone HTML mockups — the images
# alone run to megabytes, and the ones that ship go to R2 through the admin.
sketches/
# .claude/skills/ is per-machine tooling. The commands under .claude/commands/
# are shared and stay tracked; skills are not.
.claude/skills/
