node_modules
.wrangler
.env*
.dev.vars
.next
dist
**/tsconfig.tsbuildinfo
**/.turbo
.pnpm-store
.vscode/mcp.json
.mcp.json
.DS_Store
**/.obsidian
_tests
test-results/
playwright-report/
tests/screenshot/results/

# Local-only dev sketches, tuners, multi-device previews, design variants.
# Routes like /de/newsletter-variants, /de/planet-tuner, /de/footer-preview
# are work-in-progress and must never ship to production.
**/(dev)/

/.claude/worktrees
.claude-backup.tgz
/.claude/scheduled_tasks.lock

# Bible source data — operator-downloaded, do not commit ~30MB of public-domain text
scripts/bible-data/*
!scripts/bible-data/README.md
!scripts/bible-data/book-names.mjs

# Reference sketches — cloned OSS repos for study, never commit
_sketches

# Test-page assets for (dev)/3d-book — local only
apps/treasures/public/3d-book-test

# Local-only Bible mock API (used when wrangler dev hangs in the dev container)
scripts/dev-mock-api.mjs

